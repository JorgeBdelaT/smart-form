import { MultipleOptionsValidator } from './multiple-options-validator';

describe('MultipleOptionsValidator', () => {
  it('should create an instance', () => {
    expect(new MultipleOptionsValidator()).toBeTruthy();
  });
});
